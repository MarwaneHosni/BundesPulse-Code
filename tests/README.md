# Integration & cross-cutting tests

This directory is reserved for **integration** and **cross-cutting** tests that
span more than one component (e.g. backend ↔ frontend contract tests, full
snapshot round-trips). The backend's own unit tests live beside the backend in
`backend/tests/`.

Nothing here yet — tests are added in later phases.