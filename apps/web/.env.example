# API base used by the web app at build time.
# - Local dev / same-origin reverse proxy: /api
# - Vercel (frontend) → Railway (backend): full URL, e.g. https://bundespulse-api.up.railway.app
VITE_API_BASE_URL=/api