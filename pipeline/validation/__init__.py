"""Validation stage (foundation placeholder).

Schema checks, range checks, cross-checks and the source manifest generation
are added in Phase 3. The build must fail loudly rather than ship a snapshot
that violates the checks.
"""
